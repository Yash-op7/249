### Huffman Encoding

- Used for **lossless data compression**.
    
- Works by building a **binary tree (Huffman tree)** based on **symbol frequencies**.
    
- Symbols with **higher frequency get shorter codes**, rarer symbols get longer codes.
    
- **No string matching or substring search is involved**.