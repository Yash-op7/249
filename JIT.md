Just-In-Time (JIT) compilation is ==a method that compiles code into machine code while a program is running, rather than before execution==. It combines the benefits of both interpretation and traditional compilation by first interpreting code and then dynamically compiling frequently used sections, called "hot spots," into faster native machine code for improved performance. This process is commonly used in environments like Java and modern JavaScript engines.